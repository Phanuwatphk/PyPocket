# บันทึกการเปลี่ยนแปลง (CHANGELOG.md)

การเปลี่ยนแปลงที่สำคัญทั้งหมดของโครงการ PyPocket ถูกบันทึกไว้ในไฟล์นี้ โดยปฏิบัติตามรอบการพัฒนา Kanban WIP=1 ของโครงการ

---

## [v2.0.0] - 2026-08-05 — Data Persistence & Terminal UI Refactor



### สิ่งที่เพิ่มเข้ามา



* **JSON Data Persistence:** เพิ่มระบบอ่านและบันทึกประวัติรายการลงไฟล์ `transactions.json` อัตโนมัติด้วยโมดูล `json` และ `pathlib.Path` ผ่านฟังก์ชัน `load_transactions()` และ `save_transactions()` เพื่อแก้ไขปัญหาข้อมูลหายเมื่อปิดโปรแกรม


* **Terminal Screen Clearing:** เพิ่มฟังก์ชัน `clear_terminal()` โดยใช้ `os.system` เพื่อล้างหน้าจอทำให้ UI สะอาดตาเมื่อกลับสู่เมนูหลัก


* **Clear History Feature:** เพิ่มตัวเลือกเมนูที่ 5 "ล้างประวัติรายการทั้งหมด" พร้อมระบบถามยืนยัน (`y/n`) เพื่อป้องกันการเผลอลบข้อมูลโดยไม่ตั้งใจ


* **Strict Input Validation Loops:** ปรับปรุงลูปดักจับข้อผิดพลาดด้วย `while True` ซ้อนในทุกจุดรับข้อมูล (เช่น การเลือกหมวดหมู่, การยืนยันลบประวัติ, และการเลือกทำรายการถัดไป) เพื่อบังคับให้ผู้ใช้กรอกข้อมูลที่ถูกต้องโดยไม่ทำให้โปรแกรมหยุดทำงาน (Crash)



### สิ่งที่ถูกแก้ไข



* ปรับเปลี่ยนโครงสร้างฟังก์ชัน `exit()` และลูปการถามยืนยันหลังทำรายการเสร็จสิ้น ให้รองรับเฉพาะการกด Enter เพื่อไปต่อ หรือกด 0 เพื่อออกจากโปรแกรม

---

## [v1.0.0] - 2026-08-01 — Expense Category Overhaul & Menu Optimization



### สิ่งที่เพิ่มเข้ามา



* **Predefined Expense Categories:** กำหนดหมวดหมู่รายจ่ายสำเร็จรูป `EXPENSE_CATEGORIES` จำนวน 8 หมวดหมู่หลัก และเพิ่มตัวเลือกที่ 9 สำหรับกรอกหมวดหมู่เอง ("อื่นๆ")


* **Streamlined Income Entry:** ยกเลิกการถามหมวดหมู่สำหรับเมนูรายรับ โดยกำหนดค่าเป็น "รายรับ" อัตโนมัติ เพื่อลดขั้นตอนและเพิ่มความรวดเร็วในการใช้งาน

---

## [v0.2.0] - 2026-07-28 — Post-Action Flow & Exit Control



### สิ่งที่เพิ่มเข้ามา



* **Post-Action Prompt:** เพิ่มระบบถามยืนยันหลังจบแต่ละรายการ "ทำรายการต่อไปหรือออก" เพื่อให้ผู้ใช้เลือกทำรายการต่อโดยกด Enter หรือออกจากโปรแกรมโดยกด 0


* **Standardized Exit Option:** ปรับเปลี่ยนหมายเลขเมนูการออกจากโปรแกรมในเมนูหลักจากเดิมเลข 5 เป็นเลข 0 เพื่อให้เป็นมาตรฐานเดียวกันทั่วทั้งแอปพลิเคชัน

---

## [v0.1.0] - 2026-07-27 — Core Infrastructure & CLI Baseline



### สิ่งที่เพิ่มเข้ามา



* **Basic CLI Menu:** สร้างโครงสร้างเมนูควบคุมการทำงานหลักผ่าน Terminal (1. บันทึกรายรับ, 2. บันทึกรายจ่าย, 3. ดูยอดเงินคงเหลือ, 4. ดูประวัติรายการทั้งหมด)


* **In-Memory Storage:** ใช้ List of Dictionaries (`transactions = []`) ในการจัดเก็บข้อมูลการเงินชั่วคราวในหน่วยความจำ


* **Basic Input Protection:** เพิ่มระบบดักจับข้อผิดพลาด `try-except ValueError` ป้องกันโปรแกรมล่มเมื่อผู้ใช้พิมพ์ตัวอักษรลงในช่องจำนวนเงิน


* **Balance Calculation:** คำนวณยอดรวมรายรับ รายจ่าย และยอดคงเหลือสุทธิโดยใช้ List Comprehension ร่วมกับฟังก์ชัน `sum()`