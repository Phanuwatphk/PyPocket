# บันทึกการเรียนรู้ — PyPocket



**โปรเจกต์:** PyPocket
**กระบวนการทำงาน:** Kanban, จำกัด WIP = 1, ทำทีละการ์ดต่อรอบ

บันทึกนี้เก็บข้อมูลการตัดสินใจทางเทคนิค การพัฒนาตามคำสั่งที่ได้รับ และอุปสรรคในการแก้ไขบั๊กที่พบตลอดตั้งแต่เวอร์ชันแรกจนถึงเวอร์ชันล่าสุด

---

## บันทึกรอบที่ 1: โครงสร้างพื้นฐานและระบบบันทึกชั่วคราว



**เป้าหมายผลการเรียนรู้:** LO1 (การเลือกโครงสร้างข้อมูล), LO3 (การควบคุมทิศทางการทำงานแบบตอบโต้), LO4 (การเขียนโปรแกรมเชิงป้องกัน)
**คำสั่ง AI ที่ใช้:** "เริ่มเขียนโค้ดระยะที่1ได้เลย ภาษาที่ใช้: Python สิ่งที่ควรมีในโค้ด: คอมเม้นเพื่ออธิบายโค้ดแต่ส่วนให้เข้าใจ..."

**การตัดสินใจทางเทคนิคและการสะท้อนคิด:**

* เลือกใช้ List (`transactions = []`) ในการเก็บข้อมูลภาพรวม และใช้ Dictionary (`{"type": ..., "amount": ..., ...}`) เก็บรายละเอียดแต่ละรายการ เพื่อให้เข้าถึงข้อมูลได้ง่ายและเป็นระเบียบ
* สร้างสถาปัตยกรรมเมนูด้วย `while True` ควบคู่กับการใช้ `if-elif-else` เพื่อให้โปรแกรมทำงานวนซ้ำและรับคำสั่งได้อย่างต่อเนื่อง
* เพิ่มการคำนวณยอดเงินด้วย List Comprehension ร่วมกับ `sum()` ซึ่งทำให้โค้ดสั้นและมีประสิทธิภาพมากกว่าการเขียนลูป `for` แบบดั้งเดิม

**การแก้บั๊กและอุปสรรค:**

* **ปัญหา:** หากผู้ใช้กรอกข้อความแทนตัวเลขในช่องจำนวนเงิน โปรแกรมจะหยุดทำงาน (Crash) ทันที
* **การแก้ไข:** นำ `try-except ValueError` มาประยุกต์ใช้ร่วมกับคำสั่ง `continue` เพื่อดักจับข้อผิดพลาดและแจ้งเตือนผู้ใช้ให้กรอกใหม่โดยไม่หลุดออกจากโปรแกรม

---

## บันทึกรอบที่ 2: การควบคุมการไหลของหน้าจอ (Post-Action Flow)



**เป้าหมายผลการเรียนรู้:** LO3 (การควบคุมทิศทางการทำงานแบบตอบโต้)
**คำสั่ง AI ที่ใช้:** "สิ่งที่ต้องแก้ หลังจากกดเลือกเมนูและใช้ฟีเจอร์เสร็จแล้ว ให้ขึ้นคำว่า ทำรายการต่อไปหรือออก... ถ้าจะทำรายการต่อให้คลิก Enter... ถ้าจะออกให้กด 0"

**การตัดสินใจทางเทคนิคและการสะท้อนคิด:**

* เพิ่มตัวแปรรับค่า `input()` ที่ท้ายลูปใหญ่ เพื่อชะลอการกลับไปหน้าเมนูหลัก ทำให้ผู้ใช้ได้อ่านผลลัพธ์ของรายการที่เพิ่งทำเสร็จ
* ใช้เมธอด `.strip()` ควบคู่กับการตรวจสอบค่า String ว่าง `""` (เกิดจากการกด Enter) เพื่อให้ลูปวนกลับไปเริ่มต้นใหม่ได้อย่างราบรื่น
* เปลี่ยนหมายเลขเมนูออกจากโปรแกรมในหน้าแรกเป็น `0` เพื่อให้สอดคล้องกับพฤติกรรมการกด `0` ในหน้าต่างยืนยันหลังทำรายการ

**การแก้บั๊กและอุปสรรค:**

* ไม่พบอุปสรรคร้ายแรง แต่ต้องวางตำแหน่งของการถาม "ทำรายการต่อไปหรือออก" ให้อยู่นอกเงื่อนไข `if-elif` เพื่อให้ทำงานครอบคลุมทุกเมนู (1-4) ได้อย่างถูกต้อง

---

## บันทึกรอบที่ 3: ระบบหมวดหมู่แบบคงที่และไดนามิก



**เป้าหมายผลการเรียนรู้:** LO1 (การจัดการเซตข้อมูล), LO4 (การตรวจสอบเชิงป้องกัน)
**คำสั่ง AI ที่ใช้:** "อยากให้แก้ระบบหมวดหมู่ 1. ตอนเพิ่มหมวดหมู่ของรายรับให้ตัดออกเลย 2. ในส่วนของรายจ่ายให้กำหนดหมวดหมู่ไว้ในโค้ดเลย และเมื่อผู้ใช้กรอกรายจ่ายแล้ว ให้กดเลือกเมนูหมวดหมู่เอง..."

**การตัดสินใจทางเทคนิคและการสะท้อนคิด:**

* ประกาศ `EXPENSE_CATEGORIES` เป็น List ของหมวดหมู่ 8 รายการที่ตายตัว เพื่อลดภาระการพิมพ์ของผู้ใช้ และช่วยให้จัดกลุ่มข้อมูลได้ง่ายขึ้นในอนาคต
* ลดความซ้ำซ้อนในเมนูรายรับ โดยฝังค่าตัวแปร `category = "รายรับ"` เข้าไปโดยตรง ทำให้ผู้ใช้ทำงานได้รวดเร็วขึ้น
* ใช้ลูป `while True` ซ้อนเข้าไปในส่วนของการเลือกหมวดหมู่รายจ่าย เพื่อรับประกันว่าผู้ใช้ต้องเลือกตัวเลข 1-9 ที่ถูกต้องเท่านั้น หากเลือก 9 ค่อยเปิดโอกาสให้กรอกข้อมูลกำหนดเอง

**การแก้บั๊กและอุปสรรค:**

* **ปัญหา:** ผู้ใช้อาจกรอกตัวอักษรแทนตัวเลขในช่องเลือกหมวดหมู่ 1-9
* **การแก้ไข:** ใช้เมธอด `.isdigit()` เพื่อตรวจสอบว่าเป็นตัวเลขหรือไม่ ก่อนที่จะนำไปแปลงเป็น `int` เพื่อป้องกัน `ValueError`

---

## บันทึกรอบที่ 4: การจัดเก็บข้อมูลถาวรและการปรับปรุง UX (Data Persistence & UX Refactor)



**เป้าหมายผลการเรียนรู้:** LO2 (การจัดการไฟล์), LO5 (การทำเอกสารและความปลอดภัย)
**คำสั่ง AI ที่ใช้:** [มีการส่งโค้ดฉบับอัปเกรดที่มีการใช้ `json`, `pathlib`, `os` และล้างประวัติเข้ามา] "จากไฟล์นี้ช่วยวิเคราะห์และเขียน mermaid code เพื่อไป preview ใน vscode ให้หน่อย"

**การตัดสินใจทางเทคนิคและการสะท้อนคิด:**

* **Data Persistence:** เปลี่ยนผ่านจากการเก็บข้อมูลแค่ใน Memory มาเป็นการใช้ไลบรารี `json` และ `pathlib.Path` ทำให้โปรแกรมสามารถอ่านและเขียนไฟล์ `transactions.json` ได้ ข้อมูลไม่สูญหายเมื่อปิดโปรแกรม
* **UX Improvement:** นำ `os.system("cls" if os.name == "nt" else "clear")` มาสร้างฟังก์ชัน `clear_terminal()` ช่วยล้างหน้าจอเก่าทิ้งทุกครั้งที่วนกลับมาเมนูหลัก ทำให้ใช้งานง่ายและดูเป็นมืออาชีพ
* **Data Safety (Ethics):** ฟีเจอร์ใหม่ "เมนู 5: ล้างประวัติรายการทั้งหมด" เป็นคำสั่งที่อันตราย จึงออกแบบระบบยืนยัน (Confirmation Prompt) บังคับให้ผู้ใช้ต้องพิมพ์ `y/yes` หรือ `n/no` ก่อนดำเนินการลบข้อมูลจริง เพื่อปกป้องผู้ใช้จากความผิดพลาด

**การแก้บั๊กและอุปสรรค:**

* **ปัญหา:** หากไฟล์ JSON เสียหายหรือมีรูปแบบผิด โปรแกรมอาจจะล่มตอนเปิดขึ้นมา
* **การแก้ไข:** ในฟังก์ชัน `load_transactions()` ได้ใช้ `try-except (OSError, json.JSONDecodeError)` ดักจับไว้ หากเกิดปัญหาก็จะคืนค่าเป็น List ว่าง `[]` พร้อมแจ้งข้อผิดพลาดให้ผู้ใช้ทราบ ช่วยรักษาสภาพการทำงานของโปรแกรมให้ไปต่อได้

---

## การประเมินตนเองตามผลการเรียนรู้



1. **LO1** — ประยุกต์ใช้ List และ Dictionary ในการจัดเก็บโครงสร้างข้อมูลรายรับ-รายจ่ายได้อย่างมีประสิทธิภาพ


2. **LO2** — ใช้งานโมดูล `json` และ `pathlib` เพื่อการเขียน/อ่านไฟล์อย่างปลอดภัย และใช้ `sum()` เพื่อการคำนวณข้อมูล


3. **LO3** — สร้างสถาปัตยกรรมลูป `while True` ที่ซ้อนทับกัน (Nested Loops) ควบคุมทิศทางของเมนูหลักและการตรวจสอบข้อมูลได้อย่างอิสระ


4. **LO4** — เขียนโค้ดเชิงป้องกันครอบคลุมทุกจุดรับอินพุต (`try-except`, `.isdigit()`, การเช็คค่าติดลบ) ทำให้โปรแกรมมีความเสถียร (Robust) ไม่ Crash


5. **LO5** — บันทึกประวัติการพัฒนา สะท้อนแนวคิดทางสถาปัตยกรรม และคำนึงถึงความปลอดภัยของผู้ใช้งานผ่าน Confirmation Loop ก่อนทำลายข้อมูล